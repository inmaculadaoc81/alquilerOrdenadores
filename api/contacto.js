const nodemailer=require("nodemailer");const c=(v,m=4000)=>String(v??"").replace(/[<>]/g,"").trim().slice(0,m);module.exports=async(req,res)=>{if(req.method!=="POST"){res.setHeader("Allow","POST");return res.status(405).json({ok:false})}try{const b=req.body||{},nombre=c(b.nombre,120),telefono=c(b.telefono,60),email=c(b.email,180),tipo=c(b.tipo,120),duracion=c(b.duracion,120),fecha=c(b.fecha,120),mensaje=c(b.mensaje);if(!nombre||!telefono||!email||!mensaje)return res.status(400).json({ok:false});const port=Number(process.env.SMTP_PORT||465);const tr=nodemailer.createTransport({host:process.env.SMTP_HOST,port,secure:String(process.env.SMTP_SECURE??"true")==="true",auth:{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}});await tr.sendMail({from:`"Alquiler Ordenadores Madrid" <${process.env.SMTP_USER}>`,to:process.env.CONTACT_EMAIL||process.env.SMTP_USER,replyTo:email,subject:"Nueva solicitud de alquiler - alquilerordenadoresmadrid.es",text:`Nombre: ${nombre}\nTeléfono: ${telefono}\nEmail: ${email}\nTipo: ${tipo}\nDuración: ${duracion}\nFecha: ${fecha}\n\n${mensaje}`});res.status(200).json({ok:true})}catch(e){console.error(e);res.status(500).json({ok:false})}};